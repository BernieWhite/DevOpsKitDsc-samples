{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clean",
            "type": "shell",
            "command": "Remove-Item -Path .\\build -Recurse -Force; Remove-Item -Path .\\out -Recurse -Force",
            "problemMatcher": []
        }
    ]
}